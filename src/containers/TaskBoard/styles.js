const styles = (theme) => ({
  taskBoard: {
    display: "flex",
  },
  shape: {
    padding: 20,
    margin: 20,
    backgroundColor: theme.color.primary,
    color: theme.shape.textColor,
  },
  modelConfirmTextBold: {
    fontWeight: 700,
  },
});

export default styles;
